name: TrabalhoTeste

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    strategy:
      matrix:
        os: [windows-latest, ubuntu-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      # --- WINDOWS ---
      - name: Instalar dependências e compilar (Windows)
        if: runner.os == 'Windows'
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          install: mingw-w64-x86_64-gcc

      - name: Compilar no Windows
        if: runner.os == 'Windows'
        shell: msys2 {0}
        run: gcc -Wall -Wextra -O2 main.c -o programa.exe

      - name: Executar programa no Windows (com timeout)
        if: runner.os == 'Windows'
        shell: msys2 {0}
        run: timeout 5 ./programa.exe < NUL

      # --- UBUNTU ---
      - name: Instalar GCC no Ubuntu
        if: runner.os == 'Linux'
        run: sudo apt update && sudo apt install -y gcc coreutils

      - name: Compilar no Ubuntu
        if: runner.os == 'Linux'
        run: gcc -Wall -Wextra -O2 main.c -o programa

      - name: Executar programa no Ubuntu (com timeout)
        if: runner.os == 'Linux'
        run: timeout 5s ./programa < /dev/null
